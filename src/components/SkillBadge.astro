---
export interface Props {
  skill: string;
}

const { skill } = Astro.props;
---

<span class="bg-primary text-white px-4 py-2 rounded-full text-sm font-medium inline-block m-1 animate-on-scroll">
  {skill}
</span>